# 更新日志 (CHANGELOG)

## [1.0.5] - 2026-09-21

### 全链路性能与运行效率深度优化
- **事件流水线极速短路 (Short-Circuit) 调度**：
  - 将插件总开关、私聊/群聊开关判断以及机器人自身消息过滤移至流水线最顶层执行（耗时 < 0.001ms）。对于未启用私聊或自身发出的事件，无需执行任何消息链解析、图片提取、正则扫描或上下文记录，极大节省高频群聊中的 CPU 与内存消耗。
- **预编译高性能正则表达式**：
  - 模块级预编译 CQ 码图片匹配正则 `CQ_IMAGE_REGEX`，避免每次消息重复编译。
  - 在插件初始化（`__init__`）中对用户配置的 `force_trigger_regex` 与 `ignore_regex` 进行预编译缓存；若用户输入错误正则，平滑输出警告而不中断插件运行。
- **黑白名单 Set 预计算索引**：
  - 在插件启动时预先将 `session_whitelist`、`session_blacklist`、`user_whitelist`、`user_blacklist` 构建为无序哈希集合（`Set`）。每条群聊消息匹配黑白名单时直接进行集合哈希交集运算，时间复杂度从 \(O(N)\) 降低至 \(O(1)\)，避免每条消息重复遍历列表。
- **TypeSafe 结构化 Choice 问题对象常驻预缓存**：
  - 在 `MessageClassifier` 初始化时预先构造好 `should_reply`、`reply_type`、`urgency` 三个 Choice 裁决问题，避免在每条消息调用前反复生成 3 个复杂的 Choice 数据结构。

### 关键逻辑缺陷与边界容错修复
- **修复 `@全体成员` (`AtAll`) 误触 `@机器人` 的严重逻辑缺陷**：
  - 之前版本在组件匹配中包含 `AtAll`，导致群管理员发布 `@全体成员` 通告时会被误判为直接 `@机器人` 并穿透回复。现已精确剔除 `AtAll`，仅当精准 `@机器人ID`、匹配机器人别名或回复机器人消息时才激活特权。
  - 适配器兼容性增强：支持匹配 `comp.name in bot_aliases`，解决部分平台组件未携带 `qq` 字段时无法识别 `@机器人` 的问题。
- **Provider 自动回退与优雅降级保护**：
  - 在 `ReplyEngine` 中新增会话 Provider 获取失败时的兜底机制：优先调用 `context.get_using_provider_async` 获取 AstrBot 默认全局 Provider；若全局 Provider 仍未配置，输出友好警告并安全返回 `None`，杜绝未捕获的 `ProviderNotFoundError` 导致流程中断。
- **长时间持续运行防内存泄漏机制**：
  - `ContextManager`：引入容量保护门槛，当记录的活跃会话数达到 200 时，自动触发超时（1小时）会话清理；若仍超限，强制淘汰最旧的 20% 会话，杜绝数周连续挂机内存持续上涨。
  - `CooldownTracker`：新增 `cleanup_old_records`，对会话与用户冷却字典进行周期性超时修剪，并在容量超过 500 时自动淘汰过期键。
- **日志多行清洁格式化**：
  - 消息日志输出前自动清洗换行符与回车符，防止换行多行输出污染终端或造成日志格式混乱。

## [1.0.4] - 2026-09-21

### 被 @ 机器人时的处理策略增强（可选是否忽略 TypeSafe AI）
- **新增被 @ 机器人时的三种独立处理策略 (`at_bot_mode`)**：
  - **忽略 TypeSafe AI (直接调用 LLM 回复)**（默认，推荐）：显式 @ 机器人或回复机器人时，直接调用 AstrBot LLM 生成回复，跳过 TypeSafe 判定，保证高响应速度且节省 API 调用。
  - **使用 TypeSafe AI 判定**：显式 @ 机器人时同样交由 TypeSafe AI 研判意图和必要性，对于恶意骚扰、无意义刷屏可智能保持静默；若判定需要回复，则直接放行并回复。
  - **不处理，交由 AstrBot 原生处理**：本插件对 @ 机器人消息完全保持静默且不调用 `event.stop_event()`，将事件无损放行给 AstrBot 原生会话逻辑或其他对话插件处理。
- **配置与运行态全兼容**：
  - 在 `_conf_schema.json` 中提供清晰明了的中文下拉选项与帮助说明。
  - 平滑兼容旧版本 `bypass_on_at` 配置项，用户无需担心更新后配置丢失。
  - 在 `/typesafe_status` 状态面板中增加 `@机器人处理策略` 状态项展示。


### 纯媒体与图片支持、回复类型中文化与讨论类型优化
- **图片/纯媒体消息支持与多模态透传**：
  - 当关闭“忽略纯媒体/图片表情消息”（`ignore_pure_media = false`）时，自动提取消息链中的图片组件与 URL/路径。
  - 为图片消息豁免最短消息长度限制（`min_message_length`），不再被误判为超短消息默默丢弃。
  - 在 TypeSafe 判断前向 State 注入图片上下文描述，引导 Jev 模型准确识别用户发送图片/截图的互动意图。
  - 生成回复时将提取出的 `image_urls` 完整传递给 AstrBot 的 `Context.llm_generate`，由底层多模态大模型（如 Gemini、GPT-4o）直接解析图片内容并给出针对性解答。
- **允许回复类型中文化与“讨论”类型默认支持**：
  - 在 `_conf_schema.json` 中为 `allowed_reply_types` 提供完整的 14 项中文选择菜单（`options`），方便用户在 WebUI 中勾选配置。
  - 将“讨论”类型加入默认允许回复列表与 TypeSafe 提示词，支持诸如“你觉得XX怎么样”、“大家怎么看”等观点看法类话题的主动参与。
  - 自动平滑兼容历史配置：对于已安装插件使用旧版 5 项英文默认值的用户，启动时自动转换为中文并自动补齐“讨论”类型。
  - 判定静默日志中的允许回复类型列表全面改为中文展示，不再输出英文列表。


### 关键修复与模型配置新增
- **修复所有内容都在自动回复的严重判定 Bug**：
  - *根本原因*：AstrBot 框架在 `waking_check` 阶段对所有匹配了插件监听器的事件均会将内部 `event.is_wake` 置为 `True`，导致 `event.is_wake_up()` 恒为 `True`，误将所有普通群聊消息都当成了 `@机器人` 消息直接穿透生成回复，未调用 TypeSafe。
  - *修复方案*：新增 `MessageFilter.is_explicitly_at_bot` 方法，深入检查消息链中的 `At`、`AtAll`、`Reply` 机器人组件及 `@账号` 文本，彻底区分普通群消息与真实 `@机器人` 消息。
- **新增 TypeSafe 决策模型选择**：
  - 在 WebUI 设置中新增 `typesafe_model`（支持 `jev-latest (推荐最新旗舰)`、`jev`、`自定义模型`）以及 `typesafe_custom_model` 输入框。
  - SDK 请求支持按配置指定模型，并在 `/typesafe_status` 和 `/typesafe_test` 中直观展示当前模型。
- **全流程四步状态日志可视化**：
  - 在控制台输出清晰的进度状态标签：`[1/4 收到消息]`、`[2/4 调用TypeSafe]`、`[3/4 TypeSafe判决结果]`、`[4/4 决策保持静默/触发主动回复]`，让用户在后台终端对 TypeSafe 判决过程一目了然。

## [1.0.1] - 2026-09-21

### 优化与新增特性
- **设置选项中文友好显示**：WebUI 下拉与单选选项全部中文化（如“静默，不回复”、“高/中/低”、“自然”、“群友”等），后端实现中英文双向兼容归一化。
- **AI 回复思考与打字延时**：设置中新增回复延时配置组，支持随机延时（如 1~3 秒、1~5 秒）或固定秒数延时，模拟真人打字交互。
- **逻辑错误修复与加固**：
  - 修复连续回复计数器在每条用户发言时被立即重置导致的连续回复轮数上限失效 Bug。
  - 增加 `event.stop_event()` 截断，防止主动回复或 @机器人 时与 AstrBot 原生 Agent 发生双重回复。
  - 增强纯媒体检测，支持自动过滤 `[图片]`、`[动画表情]`、CQ 码等占位符，节省 API 消耗。
  - 增强会话黑白名单匹配，支持多候选 ID（group_id, session_id, unified_msg_origin）匹配不同适配器。
  - 优化 `/typesafe_test` 指令，支持解析长句与多词测试内容。

## [1.0.0] - 2026-09-21

### 初始发布
- **TypeSafe AI 消息决策**：集成 TypeSafe System One (Jev) 模型，支持多维结构化判断（`should_reply`, `reply_type`, `confidence`, `urgency`）。
- **14 种消息意图分类**：明确提问、求助、技术问题、信息查询、建议请求、讨论、闲聊、情绪表达、玩笑、陈述、打招呼、感谢、告别、其他。
- **AstrBot LLM 生成集成**：支持跟随当前会话模型或指定特定 Provider ID，配合回复风格与字数保护输出自然表达。
- **本地规则多级过滤**：
  - 会话与用户黑白名单（支持黑名单排除、仅白名单等模式，黑名单优先级最高）。
  - 消息长度上下限过滤。
  - 纯媒体消息与 AstrBot 指令消息过滤。
  - 强制触发关键词/正则与忽略关键词/正则。
- **防刷屏与智能冷却**：
  - 会话级与用户级独立冷却倒计时。
  - 最大连续主动回复次数保护。
  - @机器人 与 机器人别名识别特权穿透。
- **API 稳定性与限流**：
  - 超时与各类 HTTP/429/5xx 异常自动捕获与优雅降级。
  - 滑动窗口请求速率限制器。
  - 可选的短时间重复消息 TTL 结果缓存。
- **内置指令**：
  - `/typesafe_status`：输出插件运行状态。
  - `/typesafe_test`：测试指定消息的意图分类结果。
